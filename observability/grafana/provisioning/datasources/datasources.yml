# 데이터소스를 손으로 등록하지 않는다. 인스턴스를 다시 만들어도 그대로여야 한다 (OPS-2-18).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
